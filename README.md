# Introduction

An Instagram tool to determine which of your followers aren't following you back.

It uses the python instaloader library to interact with the Instagram API. It's designed to be used locally and requires a valid Instagram account.

## Getting Started

```bash
python -m venv .venv
source .venv/bin/activate
pip install instaloader
instaloader -l USERNAME
```
